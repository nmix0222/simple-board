# Supabase (client-safe — protected by Row Level Security, not secrecy)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-only secrets (Vercel serverless functions — NEVER expose to the client bundle)
SUPABASE_SERVICE_ROLE_KEY=
RESEND_API_KEY=
EMAIL_FROM=
ADMIN_EMAIL=
